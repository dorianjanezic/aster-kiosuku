# ===================================================================
# ASTER KIOSUKU TRADING AGENT - ENVIRONMENT CONFIGURATION
# ===================================================================
# Copy this file to .env and fill in your actual values
# All values shown are defaults or examples

# ===================================================================
# EXCHANGE API CONFIGURATION
# ===================================================================

# Aster DEX Futures API base URL
ASTER_BASE_URL=https://fapi.asterdex.com

# Aster DEX Futures API base path
ASTER_BASE_PATH=/fapi/v1

# Aster DEX WebSocket base URL for live price feeds
ASTER_WS_URL=wss://fstream.asterdex.com

# WebSocket mode: 'combined', 'paths', or 'auto'
# ASTER_WS_MODE=auto

# WebSocket path prefix (if using paths mode)
# ASTER_WS_PATH=/ws

# WebSocket streams to subscribe to (comma-separated)
# ASTER_WS_STREAMS=markPrice@1s,bookTicker,aggTrade

# ===================================================================
# EXCHANGE API CREDENTIALS (OPTIONAL - for live trading)
# ===================================================================
# Leave empty for paper/simulation trading

# Aster DEX API Key
# ASTER_API_KEY=your_api_key_here

# Aster DEX API Secret
# ASTER_API_SECRET=your_api_secret_here

# ===================================================================
# LLM/AI PROVIDER CONFIGURATION
# ===================================================================

# LLM Provider: 'grok', 'gemini', or 'noop'
LLM_PROVIDER=grok

# Primary LLM model for trading decisions
LLM_MODEL=grok-2-latest

# Model for agentic research and sentiment analysis
LLM_AGENTIC_MODEL=grok-4-fast

# Grok API Key (required for Grok provider)
# GROK_API_KEY=your_grok_api_key_here

# Grok API base URL
GROK_API_URL=https://api.x.ai/v1/chat/completions

# Grok responses URL for agentic research
GROK_RESPONSES_URL=https://api.x.ai/v1/responses

# ===================================================================
# TRADING PARAMETERS
# ===================================================================

# Maximum concurrent pairs to trade
MAX_CONCURRENT_PAIRS=10

# Trading mode: 'paper' for simulation, 'live' for real trading
MODE=paper

# ===================================================================
# PAIR SCANNING PARAMETERS
# ===================================================================

# Time interval for pair analysis (1h, 4h, 1d, etc.)
PAIRS_INTERVAL=1h

# Number of historical periods to analyze
PAIRS_LIMIT=500

# Pairs per sector/ecosystem to generate
PAIRS_PER_SECTOR=5

# Minimum correlation threshold
PAIRS_MIN_CORR=0.7

# Maximum half-life in days (strict filter)
PAIRS_MAX_HALFLIFE_DAYS=20

# Maximum half-life in days (fallback filter)
PAIRS_FALLBACK_MAX_HALFLIFE_DAYS=40

# Minimum spread Z-score for entry
PAIRS_MIN_SPREADZ=0.8

# Minimum spread Z-score (fallback)
PAIRS_FALLBACK_MIN_SPREADZ=0.5

# ===================================================================
# PAIR SCORING WEIGHTS (0.0-1.0, should sum to 1.0)
# ===================================================================

# Weight for liquidity scoring
PAIRS_W_LIQ=0.4

# Weight for volatility scoring
PAIRS_W_VOL=0.3

# Weight for funding rate scoring
PAIRS_W_FUND=0.2

# Weight for quality-volume scoring
PAIRS_W_QV=0.1

# Weight for RSI divergence scoring
PAIRS_W_RSI=0.2

# ===================================================================
# SYSTEM TIMING CONFIGURATION
# ===================================================================

# Market data refresh interval in milliseconds (default: 24 hours)
MARKETS_REFRESH_MS=86400000

# Trading loop interval in milliseconds (default: 5 minutes)
LOOP_INTERVAL_MS=300000

# ===================================================================
# CLI TOOL PARAMETERS
# ===================================================================

# Quick watch update interval in milliseconds
QUICK_WATCH_INTERVAL=5000

# Base balance for quick trading simulations
QUICK_BASE_BALANCE=10000

# ===================================================================
# DEBUGGING & LOGGING
# ===================================================================

# Enable debug logging (set to 'agent:*' for all, or specific modules)
# DEBUG=agent:orchestrator,agent:sim

# ===================================================================
# NOTES
# ===================================================================
# - For simulation/paper trading, API credentials are optional
# - For live trading, set MODE=live and provide valid API credentials
# - Adjust timing intervals based on your risk tolerance and system resources
# - Pair scanning parameters affect the quality and quantity of trading opportunities
# - Scoring weights determine how different factors influence pair selection
